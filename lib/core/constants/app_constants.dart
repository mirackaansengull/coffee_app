import 'package:flutter/material.dart';

/// Uygulama genelinde kullanılan sabitler (font, tasarım boyutu vb.).
class AppConstants {
  AppConstants._();

  static const String fontFamily = 'Poppins';

  /// ScreenUtil design size (referans ekran).
  static const Size designSize = Size(375, 812);
}
